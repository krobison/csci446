<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<h2>Considered Pets</h2>
<ul>    
  <%= render(@cart.line_items) %>
</ul>

<%= button_to 'Empty cart', @cart, method: :delete,
    data: { confirm: 'Are you sure?' } %>